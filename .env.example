# ————— قاعدة البيانات —————
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/matjarak?schema=public"

# ————— الأمان —————
# مفتاح توقيع الجلسات (JWT). ولّد واحداً عشوائياً طويلاً قبل النشر.
AUTH_SECRET="ghayyir-hatha-el-mofta7-be-nass-3ashwa2i-taweel-jiddan-32-harf-aw-akthar"

# ————— هوية المنصة —————
NEXT_PUBLIC_SITE_NAME="متجرك"
NEXT_PUBLIC_SITE_NAME_EN="Matjarak"
# النطاق الجذر. محلياً استخدم localhost:3000 (تعمل المتاجر على demo.localhost:3000)
NEXT_PUBLIC_ROOT_DOMAIN="localhost:3000"
NEXT_PUBLIC_SUPPORT_WHATSAPP="201000000000"

# ————— حساب مالك المنصة (يُنشأ عند تشغيل db:seed) —————
PLATFORM_ADMIN_EMAIL="admin@matjarak.com"
PLATFORM_ADMIN_PASSWORD="admin12345"

# ————— إشعارات واتساب (اختياري) —————
# إن تُركت فارغة: يُولَّد رابط wa.me جاهز للإرسال ويظهر في لوحة التحكم بدل الإرسال التلقائي.
WHATSAPP_API_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""

# ————— تحديد موقع الزائر (اختياري) —————
# على Vercel أو Cloudflare يُقرأ الموقع من هيدرات الـ CDN تلقائياً بلا أي إعداد.
# على استضافة أخرى: ضع رابط خدمة ترجع JSON فيه city و region، مع {ip} مكان العنوان.
# مثال: https://ipapi.co/{ip}/json/
GEOIP_LOOKUP_URL=""

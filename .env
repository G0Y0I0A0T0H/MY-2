# ————— قاعدة البيانات —————
# خادم Postgres مخصّص لهذا المشروع على المنفذ 5433 (مستقل عن خدمة PostgreSQL 18 على 5432).
# لتشغيله بعد إعادة تشغيل الجهاز:  npm run db:server:start
DATABASE_URL="postgresql://postgres:Mtj_avcDnkiPKHOoT9LYZjrsbqdN@127.0.0.1:5433/matjarak?schema=public"

# ————— الأمان —————
# مفتاح توقيع الجلسات (JWT). ولّد واحداً عشوائياً طويلاً قبل النشر.
AUTH_SECRET="w7K9JjOCixIv1k2XQ5RBdqbnZVWsPermlcSgFy6uo03UpHM8"

# ————— هوية المنصة —————
NEXT_PUBLIC_SITE_NAME="متجرك"
NEXT_PUBLIC_SITE_NAME_EN="Matjarak"
# النطاق الجذر. محلياً استخدم localhost:3000 (تعمل المتاجر على demo.localhost:3000)
NEXT_PUBLIC_ROOT_DOMAIN="localhost:3000"
# رقم واتساب التواصل الظاهر في الصفحة التسويقية (صيغة دولية: 0 تُستبدل بـ 20)
NEXT_PUBLIC_SUPPORT_WHATSAPP="201113493881"

# ————— حساب مالك المنصة (يُنشأ عند تشغيل db:seed) —————
PLATFORM_ADMIN_EMAIL="sgyiath@gmail.com"
PLATFORM_ADMIN_PASSWORD="197920"

# ————— إشعارات واتساب (اختياري) —————
# إن تُركت فارغة: يُولَّد رابط wa.me جاهز للإرسال ويظهر في لوحة التحكم بدل الإرسال التلقائي.
WHATSAPP_API_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""
